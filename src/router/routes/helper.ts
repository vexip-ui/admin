// import type { Router } from 'vue-router'

export const AsyncViewLayout = () => import('@/layouts/view')

// export function refershCurrentRoute(router: Router) {
//   const { query, params = {}, name, fullPath } = unref(router.currentRoute)

// }
